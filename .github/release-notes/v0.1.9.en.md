### Fixes

- Fixed the rename and move buttons in the top-right corner of host cards, along with the host icon and the switch button icon, disappearing after picking another shared display in the switch center.

### Known limitations

- The macOS DMG is ad-hoc signed but not notarized with an Apple Developer ID, so Gatekeeper may require manual approval.
